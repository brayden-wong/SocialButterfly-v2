export interface LoginProps {
  signup: boolean;
  setSignup: CallableFunction;
  resetPassword: boolean;
  setResetPassword: CallableFunction;
}

export interface SignupProps {
  signup: boolean;
  setSignup: CallableFunction;
}